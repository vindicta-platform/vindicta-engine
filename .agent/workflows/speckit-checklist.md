---
description: Generate a custom checklist for the current feature based on user requirements.
---
1. Read the instructions and prompt defined in `.gemini/commands/speckit.checklist.toml`.
2. Execute the workflow exactly as described in the `prompt` section of that file.
